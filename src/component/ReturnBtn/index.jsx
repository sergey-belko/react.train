import React from 'react'
import { Section } from '@component'
import style from './style.css'

export const ReturnBtn = () => (
    <Section className={style.returnBtn}>
        <a href="#top"> Go to top</a>
    </Section>
)

export default ReturnBtn
